[project]
name = "mlflow-mcp-trace-demo"
version = "0.1.0"
description = "Demo: investigate Databricks-managed MLflow traces from Claude Code via the MLflow MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlflow[mcp]>=3.5.1",
]
